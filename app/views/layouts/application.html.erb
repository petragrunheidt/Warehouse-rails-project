<!DOCTYPE html>
<html>

  <head>
    <title>WarehouseApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>
  <header>
<nav>
  <div>
  <span><h1><%= link_to 'Galpões & Estoque', root_path %></h1></span>

  <span>
  <%= link_to 'Cadastrar Galpão', new_warehouse_path %>
  </span>
  <span>
  <%= link_to 'Fornecedores', suppliers_path %>
  <%= link_to 'Cadastrar novo Fornecedor', new_supplier_path%>
  </span>
  <span>
  <%= link_to 'Modelos de Produtos', product_models_path%>
  <%= link_to 'Cadastrar novo Modelo de Produto', new_product_model_path%>
  </span>
  <span>
  <%= link_to 'Registrar Pedido', new_order_path%>
  </span>
  <span>
  <% if user_signed_in? %>
  <p>Olá, <%= current_user.description %></p>
  <%= button_to 'Sair', destroy_user_session_path, method: 'delete' %>
  <% else %>
  <%= link_to 'Entrar', new_user_session_path %>
  <% end %>
  </span>
  <% if user_signed_in? %>
  <span>
  <%= form_with url: search_orders_path, method: :get do |form|%>
    <%= form.label :query, 'Buscar Pedido' %>
    <%= form.text_field :query %>
    <%= form.submit 'Buscar'%>
  <% end %>
  <% end %>
  </span>
  <div>
</nav>
</header>
<body>

  <main>
    <div>

    <%= notice%>
    </div>
    <%= yield %>
  </main>
</body>
<footer>
<p>Criado por uma <a href='https://github.com/petragrunheidt/warehouse-app'>Sapatão</a></p>
</footer>
</html>
